% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifetable_indicators.R
\name{calc_mx}
\alias{calc_mx}
\title{Age-Specific Fecundity m_x}
\usage{
calc_mx(lt, sxj = NULL, fxj = NULL, lx = NULL)
}
\arguments{
\item{lt}{A \code{life_table} object returned by
\code{\link{read_life_table}}.}

\item{sxj}{Optional; the result of \code{\link{calc_sxj}}.}

\item{fxj}{Optional; the result of \code{\link{calc_fxj}}.}

\item{lx}{Optional; the result of \code{\link{calc_lx}}. Supplying any
of these avoids recomputing them.}
}
\value{
A data frame with the columns \code{Age} and \code{m_x}; a
  terminal row with \code{m_x = 0} is appended.
}
\description{
The age-specific fecundity of the cohort, computed from the female
age-stage survival rate, the age-specific female fecundity and the
overall survival rate: \code{m_x = s_x,Female * F_xj / l_x}. With this
definition \code{sum(l_x * m_x)} equals the net reproductive rate
\code{\link{calc_R0}}, so the Euler-Lotka equation solved by
\code{\link{calc_r}} is consistent.
}
\examples{
f <- system.file("extdata", "Example.csv", package = "insectecol")
lt <- read_life_table(f)
head(calc_mx(lt))
}
\references{
Chi, H. and Liu, H. (1985) Two new methods for the study
  of insect population ecology. \emph{Bull. Inst. Zool. Acad. Sin} 24(2), 225-240.
}
\seealso{
\code{\link{calc_fxj}}, \code{\link{calc_lx}}, \code{\link{calc_r}}
}
