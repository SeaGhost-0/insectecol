% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lifetable_save.R
\name{save_results}
\alias{save_results}
\title{Save the Results of One Life Table Analysis}
\usage{
save_results(
  lt,
  results,
  output_path = getwd(),
  plot = NULL,
  keep_tiff = FALSE,
  dpi = 300
)
}
\arguments{
\item{lt}{A \code{life_table} object returned by
\code{\link{read_life_table}}.}

\item{results}{The result list returned by \code{\link{lifeTable_calculate_all}}.}

\item{output_path}{Character; folder the workbook is written to.
Defaults to the current working directory.}

\item{plot}{A ggplot object (usually from \code{\link{plot_sxj}}); if
\code{NULL} (default) no image is exported.}

\item{keep_tiff}{Logical; whether to keep the standalone tiff file
next to the workbook in addition to the copy embedded in it. Default
\code{FALSE}, i.e. the tiff is deleted after being embedded.}

\item{dpi}{Numeric; resolution of the exported image (default 300).}
}
\value{
The path of the exported xlsx file (invisibly).
}
\description{
Writes all results of one data set into a multi-sheet Excel workbook
(\code{<file name>_out.xlsx}): the population parameters, the
age-stage survival rates, the age-specific rates and, optionally, the
survival curve plot.
}
\examples{
f <- system.file("extdata", "Example.csv", package = "insectecol")
lt <- read_life_table(f)
results <- lifeTable_calculate_all(lt)
save_results(lt, results, tempdir())
}
\seealso{
\code{\link{lifeTable_calculate}}, \code{\link{plot_sxj}}
}
